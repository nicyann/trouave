<ul class="list-group-flush">
    {% for county in counties %}
        <li>
            <a href="{{ path('show_traobject', {'id':county.id}) }}" >
                {{ county.label }}

            </a>
        </li>
    {% endfor %}


</ul>
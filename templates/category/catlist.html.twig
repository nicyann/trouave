<ul class="list-group-flush">
    {% for category in categories %}
        <li>
            <a href="{{ path('show_traobject',{'id' : category.id}) }}" >
                {{ category.label }}
            </a>
        </li>
    {% endfor %}


</ul>
<ul class="list-group-flush">
    {% for county in counties %}
        <li>
            <a href="{{ path('county', {'id':county.id}) }}" >
                {{ county.label }}

            </a>
        </li>
    {% endfor %}


</ul>